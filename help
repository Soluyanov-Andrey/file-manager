Изменили state чтоб не передовать весь во внутрь в передовать лишь нужную часть
в том месте где передается state при динамическом построении компанента
--------------------------
 в
<Line
                                key={index}
                                props={props}
                                the_main={this.state.the_main[0]}
                            />
почемуто в line переменную props переиминовать не дал