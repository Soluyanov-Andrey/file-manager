 оказывается что в конструкторе this.DataCap получается статической переменной
 и при обновлении PROPS остается постоянной, тоесть конструктор заупускается один раз и не обновляется
 в результате поступления props
 constructor(props) {
        super();
        //вот так не работало
        // const DataCap = props.props.the_main[0].DataCap
       //а вот так работало
        this.DataCap = props.props.the_main[0].DataCap

    }
